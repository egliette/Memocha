services:
  backend:
    build:
      context: backend
    image: memocha-backend
    container_name: memocha-backend
    tty: True
    ports:
      - 8055:8000
    volumes:
      - ./backend:/app

networks:
  memocha:
    external: True